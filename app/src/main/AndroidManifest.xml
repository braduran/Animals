<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.animals">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_action_icon_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_action_icon_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.Animals">
        <activity
            android:name=".MenuSideActivity"
            android:label="@string/title_activity_menu_side"
            android:theme="@style/Theme.Animals.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>